# Reference mockup provenance

Two mockups because final fig2 (鲁棒性) merges what the legacy draft series split into two figures:

- `reference_mockup_a_cross_condition.png` ← `nature_figures/fig2_cross_condition/fig2_cross_condition.png`
  (task-wise heatmap style, rank-labeled cells, DC-PSR outline highlight).
- `reference_mockup_b_cross_dataset.png` ← `nature_figures/fig3_cross_dataset/fig3_cross_dataset.png`
  (validation-ladder schematic + backbone-controlled-effect heatmaps + failure-boundary panel).

**Style-only reference.** Learn: heatmap cell/rank labeling style, orange-outline emphasis for the
proposed method, backbone-controlled delta heatmap coloring, compact panel arrangement.

**Do not reuse**: any number, rank, heatmap value, dataset composition, or conclusion. These
mockups predate the current frozen 9-method / PHM2010+NASA+MTW-CM `paper_data`. All v2 figure
content is recomputed from `paper_data` exactly as in v1 (see `../plot_fig2.py` for the
data-computation logic reused unchanged).
