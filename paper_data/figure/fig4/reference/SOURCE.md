# Reference mockup provenance

`reference_mockup.png` ← `figures/fig5_semantics_refined/fig5_semantics_refined.png`. Use
**panels (a), (b), (c), (e)** as the style reference for final fig4 (退化语义); panel (d) (the PCA
scatter) belongs to final fig5's subject matter, not fig4's — see `fig5/reference/SOURCE.md`.

**Style-only reference.** Learn: bottom-centered panel captions already implemented here, shaded
Early/Middle/Late background zones in the lifecycle panel, dashed connector between panels for a
unified visual story, hexbin+hexagonal hue-graded density styling, run-density colorbar (log
scale) on the q-agreement panel, raincloud (violin+box+jitter) styling for the wear panel.

**Do not reuse**: any q_true/q_pred value, R²/ρ/MAE numbers (this mockup happens to show numbers
close to the real ones since it's a late-stage draft, but they must still be independently
recomputed, never copied — see `../plot_fig4.py`'s validation section), VB values, or the specific
lifecycle probability curve shape shown here (it is illustrative, not the frozen C6 run).
