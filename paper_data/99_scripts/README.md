# Usage

Run build_paper_data.py from any directory, then validate_paper_data.py. build_canonical_tables.py is a convenience wrapper. Existing canonical copies are protected by prior source/canonical hashes.
