# paper_data final freeze — completion report

Completed: 2026-08-21 (Asia/Shanghai), continuing from `FINAL_FREEZE_CLEANUP_HANDOFF.md`.

## Final status

```
python paper_data/99_scripts/build_paper_data.py    -> completed, no errors
python paper_data/99_scripts/validate_paper_data.py -> VALIDATION STATUS = PASS_WITH_WARNINGS
                                                          passed=2024 warnings=4 failures=0
python paper_data/99_scripts/validate_paper_data.py -> re-run: identical result (stable)
```

The data freeze is **complete**. `paper_data/` is regenerated and validated; the four remaining warnings are expected/non-blocking (see below), not integrity failures.

## Files changed this session

Hand-edited (5 files):

- `paper_data/99_scripts/build_paper_data.py` — finished all six "work still required" items from the handoff (seed-sensitivity call wired into `main()`, dataset-naming text, D2/D3 README rewrite, MS-item split into `MANUSCRIPT_SYNC_STATUS.csv`, dynamic FIG4-001 removal, `VALIDATION_REPORT.md` excluded from its own checksum cycle, `dataset_table.csv` schema-normalization fix). One additional real bug found and fixed during the first build attempt: `Q_DEFINITIONS.md` was registered `AUTHORITATIVE` with no traceable single source file, which the strengthened validator correctly flagged; changed to `DERIVED` (it is curated text, not a copy of one source).
- `paper_data/99_scripts/validate_paper_data.py` — rewritten with the full check list from the handoff (method registry, dataset-naming separation, per-method D2/D3 recompute-vs-frozen comparison, Fig.2/Fig.4 rebuild checks, q-definition/`q_pred_norm` exclusion, seed-sensitivity isolation, manuscript-sync reported separately). Imports `build_paper_data` as a module to reuse its metric formulas instead of duplicating them.
- `nature_figures/scripts/fig4_ablation.py` — source switched from the retired `FINAL_ablation_outputs.csv` to `paper_data/01_PHM2010/03_ablation/A1_A6_AUTHORITATIVE.csv`; column names updated (`Method`→`ID`, `Output`→`Configuration`) to match. Only the data-path logic changed; layout/rendering untouched.
- `nature_figures/scripts/generate_docs.py` — Fig.4 provenance row and the D2/D3 "native universe" note updated to describe the actual frozen common-304 protocol.
- `figures/fig4_ablation/plot_fig4.py` — inspected, **not modified**: it already delegates to `plot_fig4_ablation_refined.py` and never referenced the retired file.

Everything else under `paper_data/` (360 modified + 8 new files) was regenerated by running the build script; none of it was hand-edited.

## D2/D3 recompute check (non-writing pre-build check, then verified again inside the strengthened validator)

Five methods whose source predictions were already exactly 304 rows (`rf`, `tcn_gru`, `multitask_tcn_gru`, `dc_psr`, `htt_net`): recomputed metrics match the old `metrics.json` **exactly** for both D2 and D3 — filtering to `run_id 12..315` removed nothing for these methods.

Four raw-signal methods (`multi_source_attention`, `mtf_avitk`, `dynamic_gin_tgp`, `dp2net_adapted`) had 315 source rows and are now filtered to the common 304-run universe. Deltas (old 315-row metric → new 304-row metric):

| Task/Method | Acc Δ | MacroF1 Δ | Smooth Δ | Jump Δ | Rev Δ |
|---|---:|---:|---:|---:|---:|
| D2 / multi_source_attention | −0.0046 | −0.0185 | −0.0103 | −5 | −2 |
| D2 / mtf_avitk | −0.0075 | −0.0072 | +0.0036 | 0 | 0 |
| D2 / dynamic_gin_tgp | −0.0032 | −0.0036 | −0.0005 | 0 | 0 |
| D2 / dp2net_adapted | −0.0046 | −0.0048 | +0.0014 | 0 | 0 |
| D3 / multi_source_attention | −0.0088 | −0.0052 | +0.0162 | 0 | −1 |
| D3 / mtf_avitk | −0.0057 | −0.0021 | +0.0018 | 0 | 0 |
| D3 / dynamic_gin_tgp | −0.0013 | −0.0013 | +0.0013 | 0 | 0 |
| D3 / dp2net_adapted | −0.0039 | −0.0063 | +0.0009 | 0 | 0 |

All deltas are small and in a plausible direction for dropping ~11 out-of-range runs from a raw-signal method; nothing anomalous. No duplicate `run_id`s and no cross-method `true_stage` conflicts were found on the shared universe. The strengthened validator now recomputes and checks all 18 method×task predictions files (216 metric-level checks) on every run, so this equivalence is enforced going forward, not just checked once by hand.

## Remaining genuinely unresolved items (`90_provenance/UNRESOLVED_ITEMS.csv`)

Only one item remains, and it reflects a real data limitation rather than a script bug:

- **NASA-001** — the authoritative local NASA package only contains B9–B12 for the original N1–N4 split; a nine-method NASA table is intentionally not fabricated.

(FIG4-001 is gone: the build now checks the three formal Fig.4 scripts live, and none of them reference the retired file any more.)

## Manuscript synchronization — pending, tracked separately (`90_provenance/MANUSCRIPT_SYNC_STATUS.csv`)

These are manuscript-text-vs-frozen-data mismatches, not `paper_data` integrity problems, and no longer affect `VALIDATION STATUS`:

- **MS-001** — `投稿版20260709/main.tex` Table 12 A1–A6 ablation values are stale (e.g. A1 Acc 0.9705 in the manuscript vs. 0.9901315789 in the authoritative audit).
- **MS-002** — Table 13 PHM q metrics use a different q definition than the audited `q_true`/`q_pred` statistics (canonical: R²=0.7478018753, ρ=0.9634587570, MAE=0.1132309945).
- **MS-003** — Tables 9–10 use a legacy four-task D1/D2/S1/S2 protocol that differs from the final D1/D2/D3 nine-method evidence.

These require a human decision on the manuscript text; nothing in `paper_data` should be changed to "fix" them.

## Scope boundaries respected

- Did not touch the pre-existing dirty/untracked files outside this cleanup's scope (Fig.4/5 render outputs, refined plotting scripts, MATLAB exports, the manuscript DOCX, etc.) — confirmed via `git status`/`git diff` scoped only to the 5 files above plus the files `paper_data/99_scripts/build_paper_data.py` regenerates.
- No model was trained or retrained. All D2/D3 changes are deterministic filtering + recomputation of existing frozen predictions.

## One follow-up not required for the freeze but worth knowing about

The handoff's item 2 also mentioned that the *refined* Fig.4 scripts under `figures/fig4_ablation/` (`plot_fig4_ablation_refined.py`, `plot_fig4_ablation_v2.py`, `plot_fig4_regrouped.py`, `export_fig4_v2_trajectories.py`) currently read files sitting beside the script rather than the canonical `paper_data/` paths. None of them reference the retired `FINAL_ablation_outputs.csv` (confirmed by grep), so they don't block FIG4-001 or the freeze, but repointing them to `paper_data/01_PHM2010/03_ablation/A1_A6_AUTHORITATIVE.csv` and `paper_data/07_figure_ready/fig4/` would be a good provenance-discipline cleanup for a later, separate pass — not attempted here to avoid touching plotting/rendering code that belongs to other in-flight figure work per the handoff's worktree-boundary rule.
