# 继续训练教程（给你自己跑剩下的步骤）

写这份教程的时候，进度是：
- ✅ Multi-source Attention Protocol A：已完成（5个随机种子，均值 Acc=0.8254）
- 🟡 MTF-AViTK Protocol A：已完成 **24/50** 个 epoch，检查点已保存，可以续跑
- ⬜ MTF-AViTK Protocol B（统一对比）：还没开始
- ⬜ Multi-source Attention Protocol B（统一对比）：还没开始
- ⬜ 复杂度/推理耗时基准测试：还没做
- ⬜ 最终报告填写实际数字：还没做

每一步命令**跑完之后再跑下一条**，不要同时跑两条（会抢显卡资源导致显存不够）。

---

## 第0步：打开终端

1. 按键盘上的 `Win` 键，输入 `PowerShell`，回车打开。
2. 在打开的黑色/蓝色窗口里，输入下面这行，切换到项目目录（整段复制粘贴，回车）：

```powershell
cd "C:\Users\banghai\Documents\BaiduSyncdisk\西工大\王婷\论文"
```

3. 每次运行命令前，建议先检查一下显卡有没有被占用（比如你还开着别的训练）：

```powershell
nvidia-smi
```

看输出里 `GPU-Util` 那一列，如果是个位数或者十几（比如 `0 %`、`5 %`），说明显卡是空闲的，可以继续。如果是 `30%` 以上并且持续很久，说明有别的程序在用显卡，等它跑完再继续。

---

## 第1步：跑完 MTF-AViTK Protocol A 剩下的 26 个 epoch

这是最费时间的一步，预计还要 **1-1.5 小时**（26个epoch，每个约150-180秒）。

复制粘贴下面这一整行，回车：

```powershell
& "C:\Users\banghai\miniconda3\envs\pub_baselines\python.exe" "baselines\mtf_avitk\train.py" --protocol A --device cuda --resume --epochs 50
```

**这条命令的作用**：从已经保存的第24个epoch的检查点继续训练，一直跑到第50个epoch（论文Table 5指定的训练轮数），然后自动在C6测试集上评估，把结果存到 `outputs/mtf_avitk/original_protocol/` 文件夹里。

**跑的时候你会看到什么**：屏幕上会一行一行地打印，类似这样：
```
[Protocol A/B1] resumed from epoch 24 (...)
[Protocol A/B1] epoch 24: train_loss=0.0xxx train_acc=0.99xx (156.xs)
[Protocol A/B1] epoch 25: train_loss=0.0xxx train_acc=0.99xx (155.xs)
...
```
一直到 `epoch 49` 那一行，然后会打印一行：
```
[Protocol A/B1] image-level Acc=0.9xxx (paper reports 0.9538, gap=...)
```
**看到这一行，就说明这一步彻底跑完了。**

**如果中途窗口意外关掉了、或者报错停了怎么办**：不用怕，进度不会丢。只要重新打开PowerShell，`cd` 回项目目录，把上面那条命令**原封不动再跑一遍**（带着 `--resume`），它会自动从上次保存的地方接着跑。

**怎么确认真的跑完了（不是中途卡住）**：跑完之后，输入下面这条命令查看结果文件：

```powershell
type outputs\mtf_avitk\original_protocol\metrics_summary.csv
```

如果能看到一大堆用逗号分隔的数字（Acc、Macro-F1等），就说明这一步真的完成了。

---

## 第2步：跑 MTF-AViTK Protocol B（统一对比，这是正文要用的结果）

**必须等第1步完全跑完、看到 `image-level Acc=...` 那一行之后，才能开始这一步。**

复制粘贴：

```powershell
& "C:\Users\banghai\miniconda3\envs\pub_baselines\python.exe" "baselines\mtf_avitk\train.py" --protocol B --device cuda --resume
```

**这条命令的作用**：用C1+C4训练、C6测试的统一划分（本项目的Early/Middle/Late标签），最多训练50个epoch，但有"早停"机制（如果验证集10个epoch都没有变好就会自动提前停止，不用等到50个epoch）。预计耗时 **30-60分钟**。

**跑的时候你会看到**：
```
[Protocol B] epoch 0: train_acc=0.xxxx val_run_acc=0.xxxx (best=0.xxxx@0) [xxx.xs]
[Protocol B] epoch 1: ...
```
一直跑到自动早停，或者跑满50个epoch，最后会打印：
```
[Protocol B] test C6 (run-level): Acc=0.xxxx Macro-F1=0.xxxx
```
**看到这一行就说明跑完了。**

同样地，如果中途断了，重新跑同一条命令（带 `--resume`）就会接着跑。

**验证是否跑完**：
```powershell
type outputs\mtf_avitk\unified_protocol\metrics_summary.csv
```

---

## 第3步：跑 Multi-source Attention Protocol B（统一对比）

这一步很快，模型小很多，预计 **5-15分钟**，一次性就能跑完，不用担心中途断掉的问题（就算断了，重新跑这条命令也没关系，它会从头开始重新训练，反正很快）。

复制粘贴：

```powershell
& "C:\Users\banghai\miniconda3\envs\pub_baselines\python.exe" "baselines\multi_source_attention\train.py" --protocol B --device cuda
```

跑完会打印：
```
[Protocol B] test C6: Acc=0.xxxx Macro-F1=0.xxxx
```

**验证**：
```powershell
type outputs\multi_source_attention\unified_protocol\metrics_summary.csv
```

---

## 全部跑完之后

三步都跑完、都能看到对应的 `metrics_summary.csv` 文件内容之后，**回来告诉我一声（比如说"都跑完了"）**，我会帮你：
1. 读取所有结果文件
2. 做推理耗时/参数量等复杂度测试（这个我可以自己跑，很快）
3. 把最终数字填进两份 `FINAL_REPORT.md`
4. 给你一份完整的对比报告

---

## 万一遇到报错

如果某一步命令跑完后屏幕上出现红色的 `Traceback` 或者 `Error` 字样（而不是正常的 `[Protocol ...] epoch ...` 那种打印），**不要删除任何文件**，直接把报错内容整个复制给我，我来看是什么问题。大概率不需要重新配置环境，只是某个小bug。

## 万一显存不够（报错里有 "CUDA out of memory"）

把第1步的命令改成加两个参数（显存吃紧时用）：

```powershell
& "C:\Users\banghai\miniconda3\envs\pub_baselines\python.exe" "baselines\mtf_avitk\train.py" --protocol A --device cuda --resume --epochs 50 --grad-checkpoint
```

这个 `--grad-checkpoint` 只是省显存的技巧，不会改变模型结构或训练结果的公平性（已在报告里注明）。
