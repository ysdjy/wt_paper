# Figure 5 refined — manifest pointer

The canonical machine-readable provenance record is [`data_manifest.json`](data_manifest.json).

It is regenerated by `make_fig5_semantics_refined.py` and records:

- the project audit used to establish authority;
- source paths, SHA-256 hashes, panel assignments, filters, and row counts;
- panel-specific display transformations;
- recomputed `R²`, Spearman `ρ`, MAE, stage agreement, wear summaries, and PCA variance;
- generation time and script version;
- explicit integrity statements confirming no retraining, protocol change, synthetic data, observation removal, or statistic redefinition.

This Markdown file is retained only as a human-readable compatibility pointer. Do not edit it as the authoritative manifest.
